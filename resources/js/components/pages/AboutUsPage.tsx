import React from 'react';

export default class AboutUsPage extends React.Component {
    render(): React.ReactNode {
        return (
            <h1>Welcome from about us page</h1>
        );
    }
}